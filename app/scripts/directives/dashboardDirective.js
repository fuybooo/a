define(function(require){
    var app = require('app'),
        $ = require('jquery');
    app.directive('dashboardDirective', function(){
        return {
            scope: {
            },
            link: function(scope, ele, attrs){
            }
        };

    });
});